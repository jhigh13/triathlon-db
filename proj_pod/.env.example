# Copy to proj_pod/.env and set your connection
DB_URI=postgresql+psycopg2://user:pass@host:5432/db
# Optional alias, many libs use DATABASE_URL
DATABASE_URL=${DB_URI}
# Program columns and desired program filter
PROG_ID_COL=prog_id
PROG_NAME_COL=prog_name
PROG_NAME_FILTER=Elite Men


